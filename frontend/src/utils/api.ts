/** @deprecated 浏览器 HTTP 组合已迁至 platform/browser/api；保留此入口兼容旧调用方。 */
export {
  apiFetch,
  ApiAuthenticationError,
  ApiHttpError,
  setUnauthorizedHandler
} from '../platform/browser/api';
export type { ApiRequestOptions } from '../platform/browser/api';
