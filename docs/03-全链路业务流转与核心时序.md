# 03-全链路业务流转与核心时序

本文档包含 **Sacred Focus（神圣自控中枢）** 4 大核心业务链路的交互机制、状态迁移动作与高精 Mermaid 泳道时序图，忠实还原当前前后端真实代码逻辑。

---

## 1. 神圣座位专注流 (Sacred Seat Flow)

该流转覆盖：信物启动 -> 激活沉浸式全屏 -> 30 秒物理时钟后悔药免责保护 -> 静默顺水推舟超额专注 -> 唤醒冻结时长与深潜结算。

### 1.1 业务流转时序图
对应前端源码：[`frontend/src/views/SacredSeatView.vue`](file:///d:/Codes/Projects/sacred-focus/frontend/src/views/SacredSeatView.vue)、后端路由：[`backend/src/routes/sacredSeat.ts`](file:///d:/Codes/Projects/sacred-focus/backend/src/routes/sacredSeat.ts)。

```mermaid
sequenceDiagram
    autonumber
    actor User as 用户
    participant View as SacredSeatView.vue
    participant Store as sacredSeat.ts (Pinia)
    participant Audio as audio.ts (Web Audio)
    participant API as Express API (/api/sacred-seat)
    participant DB as SQLite (better-sqlite3)

    User->>View: 确认物理信物并点击【启动专注】
    View->>View: 记录 sessionStartTime = new Date()<br/>初始化倒计时与 elapsedSeconds = 0
    View->>Store: enterFullscreen() 激活沉浸全屏
    View->>Store: isFocusMode = true (隐藏顶栏与导航)
    Note over View: 写入 localStorage: sacred_focus_active_session

    alt 场景 A: 30 秒内点击【放弃退出】(后悔药保护)
        User->>View: 点击放弃
        View->>View: 物理差值核验: (Date.now() - sessionStartTime) < regretWindowSeconds (30s)
        Note over View: 判定在后悔药保护期内! 0ms 乐观切回 IDLE 状态<br/>展示横幅: 已触发后悔药国策，无负罪感退出
        View->>API: 异步后台提交 POST /logs (status = 'REGRET')
        API->>DB: 写入 focus_session_logs (REGRET 状态不扣除连胜)
        API-->>View: 201 Created (连胜维持原值)
    else 场景 B: 超过 30 秒中途主动放弃 (违规中断)
        User->>View: 点击放弃
        View->>View: 物理差值 >= 30s! 弹出 StreakWarningModal 严正警告
        User->>View: 二次确认放弃并输入【违规诱因】
        View->>Store: resetStreak()
        View->>API: POST /logs (status = 'FAIL', failureReason = ...)
        API->>DB: 事务: 写入 FAIL 流水 + 将 sacred_seat_config.currentStreak 物理置零
        API-->>View: 201 Created (currentStreak = 0)
    else 场景 C: 顺利完成倒计时并超额深潜 (标准心流)
        View->>View: targetDuration 倒计时归零
        Note over View: 静默转入 OVER_FOCUS 顺水推舟态<br/>不发任何刺耳闹铃, 毫秒级向上累加超额秒数
        User->>View: 退出心流，首次触碰屏幕 (handleWakeUpAction)
        View->>View: 冻结采集耗时 calculateActualSeconds()<br/>弹出 PrecedentCaseModal (深潜结算弹窗)
        User->>View: 确认记录专注产出与心得
        View->>API: POST /logs (status = 'SUCCESS', actualDurationSeconds)
        API->>DB: 事务: 写入 SUCCESS 流水<br/>实际秒数 >= 60s 时 currentStreak += 1<br/>若突破记录则更新 maxStreak
        API-->>View: 201 Created (最新 currentStreak)
        View->>Audio: playChimeSound() 播放双音调水晶磬音
        View->>Store: exitFullscreen() 退出全屏，更新日级热力图
    end
```

### 1.2 核心工程防御点
1. **真实物理时钟差值防漂移 (P1-LOG-01)**：
   - 传统定时器在浏览器最小化或移动端息屏时，会被浏览器内核节流降频至数秒甚至数分钟触发一次。
   - 系统所有保护期判定和耗时计算，均调用 `Date.now() - sessionStartTime.getTime()` 读取系统物理毫秒时钟，彻底杜绝切后台导致的后悔药保护期无限拉长。
2. **离线双音调水晶磬音 (P3-AUDIO)**：
   - `playChimeSound()` 使用原生 HTML5 `AudioContext` 现场合成 $D_5$ (587.33Hz)、$A_5$ (880.0Hz)、$D_6$ (1174.66Hz) 双音调和弦与振动反馈（`navigator.vibrate`），离线零依赖、零音频加载延迟。
3. **网络故障重试队列 (P1-005)**：
   - 若提交日志时网络异常，前端捕获异常并在内存挂起 `pendingFailedLog`，界面提供【重试保存】按钮，避免用户辛勤专注的记录因偶发网络颠簸丢失。

---

## 2. 国策树拓扑流与沙盒编辑 (Focus Tree Pipeline)

该流转覆盖：展示模式打卡/反悔 -> 沙盒编辑模式 -> 拖拽布局与撤销重做 -> 保存排版前的差异比对 (Diff) 与乐观锁原子提交。

### 2.1 业务流转时序图
对应前端源码：[`frontend/src/views/FocusCanvasView.vue`](file:///d:/Codes/Projects/sacred-focus/frontend/src/views/FocusCanvasView.vue)、后端路由：[`backend/src/routes/focusTree.ts`](file:///d:/Codes/Projects/sacred-focus/backend/src/routes/focusTree.ts)。

```mermaid
sequenceDiagram
    autonumber
    actor User as 用户
    participant Canvas as FocusCanvasView.vue
    participant TreeStore as focusTree.ts (Pinia)
    participant API as Express API (/api/focus-tree)
    participant DB as SQLite 事务与外键

    alt 1. 展示模式日常打卡与反悔
        User->>Canvas: 点击国策节点打卡复选框
        Canvas->>API: PATCH /api/focus-tree/nodes/:id/toggle-lit
        API->>DB: 事务: 读取业务日状态机<br/>未点亮: level+1, 备份旧等级与旧日期, isLit=1<br/>反悔: 还原旧等级与旧业务日, isLit=0
        API->>DB: incrementSystemRevision() 原子累加版本
        API-->>Canvas: 200 OK (最新节点状态)
        Canvas->>Canvas: 局部更新节点点亮与强化徽章
    else 2. 沙盒编辑排版与保存
        User->>Canvas: 点击【编辑排版】
        Canvas->>TreeStore: beginTreeDraft() 锁定 draftBaseRevision = syncedRevision
        Canvas->>Canvas: 深拷贝克隆持久数据至 draftNodes/draftGroups/draftEdges/draftLabels<br/>开启沙盒编辑模式 isEditMode = true
        
        loop 画布拖拽与连线编辑
            User->>Canvas: 拖拽节点/外框 或 连线连接桩
            Canvas->>Canvas: pushHistory() 压入撤销快照栈 (上限 50 步)
            Canvas->>Canvas: 实时正交连线避障计算 (OrthogonalEdge)
        end

        User->>Canvas: 点击【保存排版】
        Canvas->>Canvas: 同步 flowNodes 当前坐标至草稿数据
        Canvas->>Canvas: 差异比对 (Diff): 检查 store.nodes 与 draftNodes 差集

        opt 检测到节点或外框被移出 (危险删除)
            Canvas->>Canvas: 打开 DeletionAuditModal (展示将被级联删除的节点与连线条数)
            User->>Canvas: 二次确认确认覆盖
        end

        Canvas->>TreeStore: saveWholeTree(draftTree)
        TreeStore->>API: PUT /api/focus-tree (nodes, edges, groups, labels, expectedRevision)
        
        alt 发生并发版本冲突 (OCC 409)
            API->>DB: 核验 expectedRevision != system_revision
            API-->>TreeStore: 409 Conflict (VERSION_CONFLICT)
            TreeStore-->>Canvas: 弹出预警: 云端已被其他终端修改，拒绝覆盖!
        else 正常保存成功 (200 OK)
            API->>DB: 开启事务: 清空旧连线/节点/分组/标签<br/>写入全量新结构<br/>PRAGMA foreign_key_check<br/>incrementSystemRevision()
            API-->>TreeStore: 200 OK (新 revision)
            TreeStore->>TreeStore: endTreeDraft() 释放草稿基线<br/>将草稿提交为持久数据 store.nodes
            Canvas->>Canvas: 退出编辑模式，清空历史栈
        end
    end
```

### 2.2 核心工程防御点
1. **Proxy 深拷贝克隆防内存污染**：
   - Vue 3 默认对 Store 的数组对象进行深层 Proxy 封装。若使用原生 `structuredClone` 会由于无法克隆 Proxy 对象而报错。
   - 系统历史栈与草稿初始化严格使用 `JSON.parse(JSON.stringify(...))`，确保撤销栈、草稿沙盒与持久态完全物理隔离。
2. **两步点击连接 (Click-to-Connect)**：
   - 针对移动端触摸屏或密集画布下难以精准拖拽连线的问题，实现了连接桩点击激活态（`activeConnectingHandle`）。用户先轻触起点锚点，再点击目标锚点，即可建立正交连接线，大幅降低误触率。
3. **差异比对物理删除二次审计 (`DeletionAuditModal`)**：
   - 用户在画布上误按 Backspace/Delete 删除了节点或外框，在点击保存排版时，前端会自动比对草稿与持久层的数据差集。
   - 若发现有节点或分组缺失，立即弹窗展示所有受影响的节点名称及将要级联删除的连线条数，获得用户显式确认后方可发往服务端覆写。

---

## 3. 判例法典定性裁决流 (Case Law Adjudication)

该流转覆盖：争议行为出现 -> 检索判例历史库 -> 法律裁决与定性 -> 系统广播版本推进。

### 3.1 业务流转时序图
对应前端源码：[`frontend/src/views/CaseLawView.vue`](file:///d:/Codes/Projects/sacred-focus/frontend/src/views/CaseLawView.vue)、后端路由：[`backend/src/routes/cases.ts`](file:///d:/Codes/Projects/sacred-focus/backend/src/routes/cases.ts)。

```mermaid
sequenceDiagram
    autonumber
    actor User as 用户
    participant View as CaseLawView.vue
    participant API as Express API (/api/cases)
    participant DB as SQLite (precedent_cases)

    User->>View: 遇到边界模糊行为 (例如: "专注中查询技术文档是否合规?")
    View->>API: GET /api/cases (支持按 verdict=ALLOW/FORBID 过滤)
    API->>DB: SELECT * FROM precedent_cases ORDER BY date DESC, createdAt DESC
    API-->>View: 返回全量判例法典列表
    View->>View: 客户端多字段即时全文搜索检索

    alt 未命中既有判例，需确立新法条
        User->>View: 点击【新增判例】
        View->>View: 打开 CaseEditModal
        User->>View: 录入争议行为、裁决 (ALLOW/FORBID) 及边界执行条件
        View->>API: POST /api/cases (id, date, behavior, verdict, boundaryCondition)
        API->>DB: INSERT INTO precedent_cases ...
        API->>DB: incrementSystemRevision() 原子递增全局版本号
        API-->>View: 201 Created
        View->>View: 刷新列表并弹出绿色保存成功反馈
    else 需备份判例法典
        User->>View: 点击【导出判例】
        View->>API: GET /api/cases/export
        API->>DB: 导出包含数据结构版本与全量判例的 JSON
        API-->>View: 200 OK (JSON)
        View->>User: 浏览器唤起下载 precedent-cases-<timestamp>.json
    end
```

---

## 4. 整机冷备导入与灾难恢复全链路时序 (Full System Disaster Recovery)

整机恢复属于**最高风险级别**的系统维护操作，涉及覆写 9 大业务数据表。系统为此设计了“排他维护租约 + 预热备自动容灾 + 事务级外键终验”全链路防护。

### 4.1 业务流转时序图
对应前端源码：[`frontend/src/components/common/SystemMigrationModal.vue`](file:///d:/Codes/Projects/sacred-focus/frontend/src/components/common/SystemMigrationModal.vue)、后端路由：[`backend/src/routes/system.ts`](file:///d:/Codes/Projects/sacred-focus/backend/src/routes/system.ts)。

```mermaid
sequenceDiagram
    autonumber
    actor Admin as 管理员
    participant Modal as SystemMigrationModal.vue
    participant Rate as importLimiter (10次/小时)
    participant SystemAPI as /api/system/import
    participant Maint as maintenance.ts (租约中枢)
    participant DB as SQLite 引擎 (better-sqlite3)
    participant Disk as 宿主磁盘数据目录 (/app/data)

    Admin->>Modal: 选取系统整机备份 JSON 文件
    Modal->>Modal: 前端格式初步校验
    Modal->>Rate: 发起整机导入请求 (携带 expectedRevision)
    Rate->>Rate: 核验频次 (< 10次/小时)
    Rate->>SystemAPI: 放行请求

    SystemAPI->>SystemAPI: validateFullBackupPayload() 原生强类型入参防御校验
    
    rect rgb(240, 248, 255)
        Note over SystemAPI, Maint: 步骤 1: 竞争并持有排他维护租约
        SystemAPI->>Maint: acquireMaintenanceLease('full-system-import', expectedRevision)
        Maint->>DB: 事务核验: 无活跃租约 && expectedRevision == system_revision
        Maint->>DB: 写入 system_meta (key='maintenance_lock', TTL=15min)
        Maint-->>SystemAPI: 成功获得租约 ownerId
    end

    rect rgb(255, 245, 238)
        Note over SystemAPI, Disk: 步骤 2: 灾难恢复硬核保障 —— 预导入快照热备
        SystemAPI->>DB: db.backup('/app/data/app_pre_import_<timestamp>.db')
        DB->>Disk: 在线将当前整个运行中数据库生成物理热备镜像
        SystemAPI->>Disk: prunePreImportBackups() 自动淘汰历史热备 (保留最新 5 个)
    end

    rect rgb(245, 255, 245)
        Note over SystemAPI, DB: 步骤 3: 开启 SQLite 原子覆盖事务
        SystemAPI->>DB: 开始 IMMEDIATE 事务
        SystemAPI->>Maint: assertMaintenanceLease(lease) 核验租约依然有效
        SystemAPI->>DB: DELETE FROM focus_edges / nodes / groups / labels
        SystemAPI->>DB: DELETE FROM focus_session_logs / precedent_cases / evolution_*
        SystemAPI->>DB: INSERT 全量恢复数据 (国策树、座位配置、流水日志、判例、演化快照)
        SystemAPI->>DB: PRAGMA foreign_key_check (严格核验无任何外键破坏)
        SystemAPI->>DB: incrementSystemRevision() 递增版本号
        SystemAPI->>DB: 提交事务 (COMMIT)
    end

    rect rgb(240, 248, 255)
        Note over SystemAPI, Maint: 步骤 4: 释放维护租约
        SystemAPI->>Maint: releaseMaintenanceLease(lease)
        Maint->>DB: DELETE FROM system_meta WHERE key='maintenance_lock'
    end

    SystemAPI-->>Modal: 200 OK (返回恢复实体条数摘要统计)
    Modal->>Modal: 触发全栈数据刷新，提示整机恢复成功
```

### 4.2 灾难回退保障细节
* **导入前热备失败阻断 (P1-002)**：如果因为磁盘空间不足等原因导致 `db.backup()` 抛错，系统**立刻终止导入流程并主动释放维护租约**，严禁在没有安全快照的情况下执行破坏性覆写。
* **物理热备回退手册**：若导入了包含逻辑错误的数据包，宿主管理员只需进入 `/app/data/` 目录，将生成的 `app_pre_import_<timestamp>.db` 复制替换回 `app.db` 并重启容器，即可 100% 毫发无损地回退到恢复发起前的绝对状态。

---

## 5. 本文档涉及的真实源码文件映射

* 神圣座位业务视图：[`frontend/src/views/SacredSeatView.vue`](file:///d:/Codes/Projects/sacred-focus/frontend/src/views/SacredSeatView.vue)
* Web Audio 音效合成：[`frontend/src/utils/audio.ts`](file:///d:/Codes/Projects/sacred-focus/frontend/src/utils/audio.ts)
* 国策画布核心视图：[`frontend/src/views/FocusCanvasView.vue`](file:///d:/Codes/Projects/sacred-focus/frontend/src/views/FocusCanvasView.vue)
* 差异比对审计弹窗：[`frontend/src/components/canvas/DeletionAuditModal.vue`](file:///d:/Codes/Projects/sacred-focus/frontend/src/components/canvas/DeletionAuditModal.vue)
* 判例法典核心视图：[`frontend/src/views/CaseLawView.vue`](file:///d:/Codes/Projects/sacred-focus/frontend/src/views/CaseLawView.vue)
* 全系统整机恢复路由：[`backend/src/routes/system.ts`](file:///d:/Codes/Projects/sacred-focus/backend/src/routes/system.ts)
* 排他维护租约控制：[`backend/src/db/maintenance.ts`](file:///d:/Codes/Projects/sacred-focus/backend/src/db/maintenance.ts)
